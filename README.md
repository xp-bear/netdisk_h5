# 小熊云盒 - H5 移动端

<div align="center">

![Logo](./src/assets/logo.png)

基于 Vue 3 + Vant 4 打造的现代化移动端云存储应用

[![Vue](https://img.shields.io/badge/Vue-3.4-brightgreen.svg)](https://vuejs.org/)
[![Vant](https://img.shields.io/badge/Vant-4.8-blue.svg)](https://vant-ui.github.io/)
[![Vite](https://img.shields.io/badge/Vite-5.2-646CFF.svg)](https://vitejs.dev/)
[![License](https://img.shields.io/badge/License-MIT-orange.svg)](LICENSE)

[在线预览](http://m.pan.coderxp.top/) | [PC 端](http://pan.coderxp.top/)

</div>

---

## 📱 项目简介

小熊云盒是一款仿百度网盘的移动端应用，提供完整的云存储、文件管理、分享广场等功能。项目采用 Vue 3 生态系统，配合 Vant 4 移动端组件库，提供流畅的用户体验。

### ✨ 特色功能

- 🎯 **智能设备检测** - 自动识别 PC/移动端访问，智能跳转对应版本
- 🎨 **精美 UI 设计** - 现代化界面，流畅动画效果，支持暗色模式
- 📂 **完整文件管理** - 支持文件上传、预览、下载、删除、移动等操作
- 🔄 **实时数据同步** - 基于 Pinia 状态管理，数据实时更新
- 🎭 **个性化图标** - 自定义 Tabbar 图标，选中时弹跳动画效果
- 📊 **存储空间管理** - 实时显示空间使用情况，可视化进度条
- 🌐 **分享广场** - 文件分享到社区，浏览他人分享的资源
- 🖼️ **头像裁剪** - 内置图片裁剪功能，自定义用户头像
- 🔍 **智能筛选** - 支持按文件类型筛选，快速定位目标文件
- 📱 **移动端优化** - 下拉刷新、上拉加载、触摸手势等移动端交互

---

## 🛠️ 技术栈

### 核心框架
- **Vue 3** - 采用 Composition API 的现代化前端框架
- **Vite** - 极速的前端构建工具
- **Vue Router** - 官方路由管理器，支持路由守卫
- **Pinia** - 新一代状态管理库，替代 Vuex

### UI 组件
- **Vant 4** - 轻量级移动端组件库
- **自定义组件** - FileIcon、头像裁剪器等

### 工具库
- **Axios** - Promise 风格的 HTTP 请求库
- **Ali-OSS** - 阿里云对象存储 SDK
- **Cropper.js** - 图片裁剪库

---

## 📦 项目结构

```
h5/
├── dist/                    # 构建产物
├── src/                     # 源代码
│   ├── api/                 # API 接口
│   │   └── index.js         # 统一的 API 请求封装
│   ├── assets/              # 静态资源
│   │   ├── logo.png         # 应用 Logo
│   │   ├── favicon.svg      # 网站图标
│   │   ├── icons/           # 文件类型图标
│   │   └── tabbar/          # Tabbar 图标
│   │       ├── home.svg / home-active.svg
│   │       ├── upload.svg / upload-active.svg
│   │       ├── share.svg / share-active.svg
│   │       └── my.svg / my-active.svg
│   ├── components/          # 公共组件
│   │   └── FileIcon.vue     # 文件类型图标组件
│   ├── router/              # 路由配置
│   │   └── index.js         # 路由定义、守卫、滚动行为
│   ├── stores/              # Pinia 状态管理
│   │   ├── user.js          # 用户状态（登录、个人信息）
│   │   └── file.js          # 文件状态（文件列表、文件夹）
│   ├── utils/               # 工具函数
│   │   └── oss.js           # OSS 文件上传、格式化工具
│   ├── views/               # 页面视图
│   │   ├── Login.vue        # 登录/注册页
│   │   ├── Home.vue         # 文件管理页（首页）
│   │   ├── Upload.vue       # 上传页
│   │   ├── Share.vue        # 分享广场页
│   │   └── Profile.vue      # 个人中心页
│   ├── App.vue              # 根组件
│   └── main.js              # 应用入口，包含设备检测
├── index.html               # HTML 模板
├── vite.config.js           # Vite 配置（代理、别名）
├── package.json             # 项目依赖
└── README.md                # 项目说明
```

---

## 🚀 快速开始

### 环境要求

- Node.js >= 16.0.0
- npm / pnpm / yarn

### 1. 安装依赖

```bash
# 使用 npm
npm install

# 使用 pnpm（推荐）
pnpm install

# 使用 yarn
yarn install
```

### 2. 配置阿里云 OSS

编辑 `src/utils/oss.js` 文件，填入你的阿里云 OSS 配置：

```javascript
const ossConfig = {
  region: 'oss-cn-beijing',          // OSS 区域
  accessKeyId: 'your-access-key-id', // AccessKey ID
  accessKeySecret: 'your-secret',     // AccessKey Secret
  bucket: 'your-bucket-name'          // Bucket 名称
}
```

### 3. 启动开发服务器

```bash
npm run dev
```

项目将运行在 `http://localhost:5174`

### 4. 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist/` 目录

### 5. 预览生产版本

```bash
npm run preview
```

---

## 🎨 核心功能详解

### 1. 用户认证系统
- ✅ 用户注册（邮箱/用户名）
- ✅ 用户登录（记住我功能）
- ✅ 自动登录（token 持久化）
- ✅ 路由守卫（未登录自动跳转）
- ✅ 退出登录

### 2. 文件管理
- ✅ **文件上传**
  - 支持多文件同时上传
  - 实时显示上传进度
  - 支持取消上传
  - 自动同步到云端（阿里云 OSS）
  
- ✅ **文件列表**
  - 下拉刷新
  - 上拉加载更多
  - 文件类型图标展示
  - 文件大小、时间显示
  
- ✅ **文件操作**
  - 文件预览（图片、视频、音频、PDF）
  - 文件下载
  - 文件删除
  - 文件重命名
  - 批量移动文件
  - 文件搜索
  
- ✅ **文件夹管理**
  - 创建文件夹
  - 重命名文件夹
  - 删除文件夹（递归删除）
  - 面包屑导航
  - 文件夹层级浏览

### 3. 分享广场
- ✅ 文件分享到广场
- ✅ 浏览分享广场文件
- ✅ 按文件类型筛选（图片、视频、文档、音频等）
- ✅ 保存分享文件到个人网盘
- ✅ 文件预览（支持图片、视频、音频）
- ✅ 下拉刷新，上拉加载

### 4. 上传管理
- ✅ 最近上传文件列表
- ✅ 实时上传进度显示
- ✅ 上传历史记录
- ✅ 快速访问已上传文件

### 5. 个人中心
- ✅ **个人信息**
  - 用户名、邮箱展示
  - 头像上传和裁剪
  - 个人资料编辑（昵称、性别、生日、签名）
  - 修改密码
  
- ✅ **存储管理**
  - 存储空间使用情况
  - 可视化进度条
  - 已用/总容量显示
  
- ✅ **系统功能**
  - 清除缓存
  - 关于我们
  - 退出登录

### 6. UI/UX 优化
- ✅ **Tabbar 动画** - 选中时图标弹跳缩放效果
- ✅ **滚动重置** - 页面切换时自动滚动到顶部
- ✅ **智能跳转** - PC 端访问自动跳转到 PC 版
- ✅ **空状态提示** - 无数据时的友好提示
- ✅ **加载动画** - 数据加载时的占位效果
- ✅ **错误处理** - 统一的错误提示

---

## 🌐 API 接口

### 后端服务

项目使用 `end` 文件夹中的 Node.js 后端服务。

**默认地址：**
- 开发环境：`http://localhost:6001/api`（通过 Vite 代理）
- 生产环境：`http://120.48.51.185:6001/api`

### 接口列表

#### 用户认证
```
POST   /api/auth/register      # 用户注册
POST   /api/auth/login         # 用户登录
GET    /api/auth/user          # 获取用户信息
PUT    /api/auth/user          # 更新用户信息
PUT    /api/auth/password      # 修改密码
```

#### 文件管理
```
GET    /api/files              # 获取文件列表
POST   /api/files              # 创建文件记录
DELETE /api/files/:id          # 删除文件
PUT    /api/files/:id/move     # 移动文件
PUT    /api/files/:id/rename   # 重命名文件
```

#### 文件夹管理
```
GET    /api/folders            # 获取文件夹列表
POST   /api/folders            # 创建文件夹
PUT    /api/folders/:id        # 重命名文件夹
DELETE /api/folders/:id        # 删除文件夹
```

#### 分享功能
```
POST   /api/share/files/:id   # 分享文件到广场
DELETE /api/share/files/:id   # 取消分享
GET    /api/share/square       # 获取分享广场文件
POST   /api/share/save/:id     # 保存分享文件到网盘
```

#### 存储统计
```
GET    /api/storage/stats      # 获取存储空间统计
```

---

## 📱 移动端适配

### 响应式设计
- 使用 viewport meta 标签禁止缩放
- 基于 Vant 组件库的响应式设计
- 适配不同屏幕尺寸（375px - 768px）

### 移动端交互
- ✅ 下拉刷新
- ✅ 上拉加载更多
- ✅ 触摸手势（长按、滑动）
- ✅ 原生返回键监听
- ✅ 弹窗层级管理

### 性能优化
- ✅ 图片懒加载
- ✅ 路由懒加载
- ✅ 组件按需引入
- ✅ 防抖节流处理
- ✅ 虚拟滚动（大列表优化）

---

## 🔧 配置说明

### Vite 配置 (`vite.config.js`)

```javascript
export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': fileURLToPath(new URL('./src', import.meta.url))
    }
  },
  server: {
    host: '0.0.0.0',
    port: 5174,
    proxy: {
      '/api': {
        target: 'http://localhost:6001',
        changeOrigin: true
      }
    }
  }
})
```

### 路由配置 (`router/index.js`)

```javascript
// 路由守卫 - 未登录跳转到登录页
router.beforeEach((to, from, next) => {
  const userStore = useUserStore()
  if (to.meta.requiresAuth && !userStore.token) {
    next('/login')
  } else {
    next()
  }
})

// 滚动行为 - 页面切换时回到顶部
scrollBehavior(to, from, savedPosition) {
  return { top: 0, behavior: 'instant' }
}
```

---

## 🎯 设备检测

项目内置智能设备检测功能，在 `main.js` 中实现：

```javascript
// 检测是否为移动端
const isMobile = () => {
  const userAgent = navigator.userAgent.toLowerCase()
  const mobileKeywords = ['android', 'iphone', 'ipad', 'ipod', 'windows phone', 'mobile']
  const isMobileDevice = mobileKeywords.some(keyword => userAgent.includes(keyword))
  const isSmallScreen = window.innerWidth <= 768
  return isMobileDevice || isSmallScreen
}

// PC 端访问自动跳转
if (!isMobile()) {
  window.location.href = 'http://pan.coderxp.top/'
}
```

**跳转逻辑：**
- 📱 移动端访问 H5 → 正常显示
- 💻 PC 端访问 H5 → 自动跳转到 PC 版

---

## 🐛 注意事项

1. **后端服务** - 需要先启动后端服务才能正常使用
   ```bash
   cd ../end
   npm install
   npm start
   ```

2. **OSS 配置** - 需要配置阿里云 OSS 才能使用文件上传功能

3. **开发调试** - 建议使用 Chrome DevTools 的移动端模拟器
   - F12 打开开发者工具
   - 点击设备工具栏（Ctrl+Shift+M）
   - 选择移动设备型号

4. **真机测试** - 推荐使用真机进行最终测试
   - 使用 `npm run dev` 启动开发服务器
   - 访问局域网 IP（如 http://192.168.1.100:5174）

5. **生产环境** - 部署前需要修改 API 地址
   - 修改 `vite.config.js` 中的代理配置
   - 或在 `api/index.js` 中直接修改 baseURL

---

## 📝 开发建议

### 代码规范
- 使用 ESLint 进行代码检查
- 使用 Prettier 进行代码格式化
- 遵循 Vue 3 Composition API 最佳实践

### 性能优化
- 避免在移动端预览大文件
- 图片使用压缩后的版本
- 合理使用缓存策略
- 注意内存泄漏问题

### 调试技巧
- 使用 Vue DevTools 调试组件状态
- 使用 Network 面板监控网络请求
- 使用 Console 查看错误日志
- 使用 Performance 分析性能瓶颈

---

## 🔗 相关链接

- [在线预览（移动端）](http://m.pan.coderxp.top/)
- [在线预览（PC 端）](http://pan.coderxp.top/)
- [Vue 3 文档](https://cn.vuejs.org/)
- [Vant 4 文档](https://vant-ui.github.io/vant/#/zh-CN)
- [Vite 文档](https://cn.vitejs.dev/)
- [Pinia 文档](https://pinia.vuejs.org/zh/)

---

## 📄 开源协议

本项目基于 [MIT](LICENSE) 协议开源。

---

## 👥 贡献者

欢迎提交 Issue 和 Pull Request！

---

<div align="center">

**如果觉得这个项目不错，欢迎 Star ⭐️**

Made with ❤️ by CoderXP

</div>
